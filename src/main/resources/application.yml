spring:
  application:
    name: micro-order
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:order
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 8083